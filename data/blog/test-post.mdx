---
title: "Markdown/MDX with Next.js"
date: "2021-06-21"
summary: "klsjafhaskldjhfaskjfhas"
author: "Daniel Hangan"
---

# Hello World

hope this is working fine.

```tsx
export default class App extends NextApp {
  render() {
    const { Component, pageProps } = this.props;
    return (
      <ThemeProvider theme={theme}>
        <ColorModeProvider value="light">
          <CSSReset />
          <Component {...pageProps} />
        </ColorModeProvider>
      </ThemeProvider>
    );
  }
}
```
